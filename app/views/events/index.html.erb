<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%=render 'feed'%>
  </tbody>
</table>

<br>
<% if user_signed_in? %>
<div class="text-center">
<%= link_to 'New Event', new_event_path, class: "btn btn-info" %>
</div>
<% end %>
